# Provisioned automatically by the Vercel Marketplace integrations.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_example
CLERK_SECRET_KEY=sk_test_example

# Local-only actor bypass. Ignored when NODE_ENV=production.
VLE_DEV_ACTOR=ops@vle.exchange

# Production TECRID transport is intentionally unconfigured until TECRID signs
# the endpoint, authentication, evidence, and revocation contract. See docs/TECRID_ADAPTER.md.
